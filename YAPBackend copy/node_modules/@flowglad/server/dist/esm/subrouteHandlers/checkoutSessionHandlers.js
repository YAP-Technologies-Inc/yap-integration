import {
  __async
} from "../chunk-JPRRZPBL.js";
import { HTTPMethod } from "@flowglad/shared";
import { parseErrorStringToErrorObject } from "../serverUtils";
const createCheckoutSession = (params, flowgladServer) => __async(void 0, null, function* () {
  let error;
  let status;
  let data = {};
  if (params.method !== HTTPMethod.POST) {
    error = {
      code: "Method not allowed",
      json: {}
    };
    status = 405;
    return {
      data,
      status,
      error
    };
  }
  try {
    const checkoutSession = yield flowgladServer.createCheckoutSession(params.data);
    data = checkoutSession;
    status = 200;
  } catch (e) {
    if (e instanceof Error) {
      error = parseErrorStringToErrorObject(e.message);
    } else {
      error = {
        code: "Unknown error",
        json: {}
      };
    }
    status = 500;
  }
  return {
    data,
    status,
    error
  };
});
export {
  createCheckoutSession
};
//# sourceMappingURL=checkoutSessionHandlers.js.map